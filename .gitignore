# ── Python ──────────────────────────────
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# ── uv / 가상환경 ───────────────────────
.venv/

# ── 환경설정 (secrets / 로컬 설정) ──────
.env

# ── 수동 원가 보정 (기밀: 공급단가 포함) ──
cost_overrides.csv

# ── 기밀 데이터 & 산출물: 모든 Excel/PDF 제외 ──
# (원본 매출·매입 자료, 생성된 월별 보고서 전부 — 기밀)
*.xlsx
*.xls
*.pdf

# ── 참조 번들 / 기타 ────────────────────
pyproject.zip

# ── MS Office 임시/잠금 파일 ─────────────
~$*

# ── OS ──────────────────────────────────
.DS_Store
Thumbs.db
